# Workflows

Use this directory to describe recurring maintenance workflows for this hardware repository.

Suggested future workflows:

- verify current support matrix after kernel updates
- retest camera streaming after IPU6 or HAL changes
- revalidate fingerprint flow after `libfprint` updates
